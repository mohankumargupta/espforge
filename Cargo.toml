[workspace]
resolver = "2"
members = ["espforge", "espforge_examples" ,"espforge_macros", "espforge_templates"]

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
