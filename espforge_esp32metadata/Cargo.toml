[package]
name = "espforge_esp32metadata"
version = "0.1.0"
edition = "2024"
description = "Embedded examples for espforge"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.9"

[build-dependencies]
toml = "0.9"

