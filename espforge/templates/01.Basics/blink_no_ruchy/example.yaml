espforge:
  name: blink_no_ruchy
  platform: esp32c3

esp32:
  gpio:
    gpio2: { pin: 18, direction: output }

components:
  red_led:
    using: LED
    with:
      gpio: $gpio2

app:
  setup:
    - log.info: Hello Blink Ruchy

  loop:
    - $red_led.toggle: 
    - delay.delay_millis: 500

