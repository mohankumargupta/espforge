espforge:
  name: blink
  target: esp32c3
  runtime: blocking

esp32:
  gpio:
    gpio18: { pin: 18, direction: output }

components:
  red_led:
    using: led
    with:
      pin: $gpio18

devices: []
