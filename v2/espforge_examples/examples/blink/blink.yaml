espforge:
  name: blink
  target: esp32c3
  runtime: blocking

esp32:
  gpio:
    gpio2: { pin: 18, direction: output }

components:
  - id: red_led
    using: led
    with:
      pin: $gpio2

devices: []
